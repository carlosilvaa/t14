import React,{createContext,useContext,useState} from 'react'; type U={email:string;name?:string}|null; type C={user:U;login:(u:U)=>void;logout:()=>void}; const Ctx=createContext<C>({user:null,login:()=>{},logout:()=>{}}); export function AuthProvider({children}:{children:React.ReactNode}){const [user,setUser]=useState<U>(null); return <Ctx.Provider value={{user,login:setUser,logout:()=>setUser(null)}}>{children}</Ctx.Provider>} export function useAuth(){return useContext(Ctx)}